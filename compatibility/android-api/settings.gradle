pluginManagement {
  plugins {
    id "org.jetbrains.kotlin.android" version providers.gradleProperty("kotlinVersion").orElse("1.9.22").get()
  }

  repositories {
    google()
    mavenCentral()
    gradlePluginPortal()
  }
}

dependencyResolutionManagement {
  repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
  repositories {
    google()
    mavenCentral()
  }
}

rootProject.name = "react-native-bs-diff-patch-android-api-compatibility"
